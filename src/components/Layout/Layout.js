import React from 'react';
import {Body} from '../Body/Body';
import {Header} from '../Header/Header';

export const Layout = () => {
  return (
    <div>
      <Header />
      <Body />
    </div>
  );
}

